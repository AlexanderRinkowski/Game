class CombatManager {
    // manipulates the board using boardmanager according to autobattler rules
    constructor(boardManager) {
        this.boardManager = boardManager;
    };

    startCombat(row){};

    wizardCombat(){};
    
    closeCombat(){};
}